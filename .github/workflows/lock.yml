name: Lock closed issues and PRs (disabled in fork)

# explicitně nespouštět nic
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the fork."
